apiVersion: v1
kind: Service
metadata:
  name: microdeploy-api
  namespace: microdeploy
  labels:
    app: microdeploy-api
spec:
  selector:
    app: microdeploy-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080